/*
Kth largest sum (also use Priority Queue here)
Max/min suarray sum
Max/min subarray product
Rotated Array Search
Rotate an array
Subsequence
Target Sum
Binary Search
Add-One (to a big number represented as array)
matrix multiplication
Quick/Merge/Heap/Radix/Shell/Selection/Bubble/Insertion Sorts
sliding window problems
implement stack/ queue/ priority Queue/ graph using adjacency matrix
*/
class Program
{
    static void Main(string[] args)
    {
        // int[] array_2x3 = { { 1, 2, 3 }, { 4, 5, 6 } };
        int[,] array_3x1 =
        {
            {1},
            {2},
            {3}
        };
    }
    static void MatrixMultiplication(int[,] array1, int[,] array2)
    {

        if (array1.GetLength(1) != array2.GetLength(0))
        {
            return;
        }
        
        // int[,] ans = new int[array1.GetLength(0), array2.GetLength(1)];

        // for (int row = 0; row < array1.GetLength(0); row++)
        // {
        //     for (int col = 0; array2.GetLength(1); col++)
        //     {

        //     }
        // }



    }
}